let addBtn=document.querySelector(".add-btn");
console.log(addBtn);

var isModalPresent = false;
addBtn.addEventListener("click", function() {
    // case 1-> if screen is not present
    //          then display modal

if(!isModalPresent){
    // display modal
}

    // case 2-> if modal is present
    //          then hide modal

else{
    // display none
}


isModalPresent= !isModalPresent;

})